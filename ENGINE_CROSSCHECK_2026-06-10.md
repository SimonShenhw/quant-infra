# v13-C 双引擎交叉验证报告（2026-06-10）

> 动机：arXiv:2603.20319 表明高换手策略在不同回测引擎间加入现实成本后结果可分歧达 3.71pp。
> 本报告用独立第二引擎复算 `run_v13_final.py` 配置 C（banded top-3/bottom-3 long-short，每日决策）
> 并量化引擎间分歧。工具与产物见文末清单；现有代码零改动。

---

## ① 结论（可直接引用的披露区块）

**v13-C 业绩披露应同时给出三个数字：**

| 口径 | 总收益 | Sharpe | PSR | maxDD | 说明 |
|---|---|---|---|---|---|
| Headline（原引擎 + TWAP/adverse-selection 成本模型） | **+32.63%** | 0.806 | 0.840 | 23.4% | 实现均价成本约 5bps/边 |
| 同引擎 + 固定费率 taker 4bps + 滑点 4bps/边 | **+19.52%** | 0.561 | 0.755 | 24.7% | 成本模型敏感度 **−13.1pp** |
| **保守下界（独立引擎 + 固定 8bps/边 + 全簿对齐目标权重）** | **+18.21%** | **0.534** | 0.744 | 24.9% | 建议对外引用的下界 |

- **引擎机制分歧：−0.79pp ～ +1.31pp**（固定费率下，取决于交易集口径，见 ③），
  与文献 3.71pp 同量级但更小；**成本模型选择（TWAP 模拟 vs 固定 8bps）才是头号自由度（13.1pp）**。
- 任何场合引用 +32.6% 时，必须注明其成本模型为 TWAP + adverse-selection 模拟
  （maker 1bp×20% + adverse 6bp×51% + taker 6bp×29% ≈ 5bps/边），并附上 +18.2% 保守下界。

## ② 验证链（证明对比的是引擎机制而非策略错配）

1. **OOS 预测矩阵重建**：`checkpoints/folds_v13/` 15 个 fold ckpt + 原数据管线
   （`build_from_parquet`，13,105 样本 × 20 资产 × 19 因子）+ 与 `run_cpcv` 相同的
   CPCV 切分（purge=48, embargo=48）逐 fold 推理集成。
2. **原引擎复现**：重建预测喂回原 `run_backtest`（原 TWAP 成本、`random.seed(42)`）
   → **+32.6337%，与 `checkpoints/v13_backtest_summary.json` 分歧 0.0000pp**，
   490 次调仓 / 2,092 笔 slot 交易逐项一致。
3. **决策序列提取**：复用原 `_banded_targets` 回放 banded 状态机，事件数与交易数
   与引擎 1 完全吻合 → 两引擎按构造交易**同一决策序列**。
4. **语义复刻校验**：引擎-1 记账方式（恒权重复利 + 决策 bar 计提成本 + ±10% 截断）
   在提取决策上的 numpy 复刻 vs 引擎-1 固定费率版：**0.000000pp** → 成交计时
   （决策后下一根 close 生效）两边严格一致，可从分歧原因中排除。

## ③ 引擎间分歧与归因（固定 taker 4bps + 滑点 4bps/边）

第二引擎为纯 numpy 手写（环境无 vectorbt 且禁止安装）：按持仓**单位**记账、逐 bar
对 close 矩阵盯市、成本按**实际成交名义额**于成交时从现金扣除、调仓间隙持仓自然漂移。
两种交易集口径：

| 对比 | 分歧 | bar 收益相关 | max \|log 净值差\| |
|---|---|---|---|
| E1 vs E2-changed（只交易换槽 = 引擎 1 实际收费的交易集） | **−0.79pp**（E2 高） | 0.988 | 0.031 |
| E1 vs E2-full（每次调仓全簿对齐 ±1/6 = vectorbt 类引擎惯例） | **+1.31pp**（E2 低） | 0.997 | 0.020 |

两者均超过 0.5pp 阈值。fee=0 对照拆解：

| 成因 | changed | full | 机制 |
|---|---|---|---|
| 复利/盯市方式 | **−0.78pp** | +0.13pp | 引擎 1 每 bar 把权重隐式**无成本**再归一到 ±1/6；真实持仓漂移（赢家放大）本样本反而多赚 |
| 成本计提口径 | −0.01pp | **+1.18pp** | 引擎 1 只对进/出槽收费，从不为维持恒定权重所需的 true-up 交易付费（成本 261.2k vs 268.8k）——PnL 假设与成本模型**内部不一致** |
| 成交计时 | ~0 | ~0 | 由 ② 第 4 步复刻校验排除 |

次要因素：引擎 1 的 ±10% 单 bar 收益截断在固定费率路径上实际触发 **1 次**
（t=9693，+10.13% → +10.00%，对 E1 约 −0.13pp），已包含在"复利/盯市方式"项中；
独立引擎无截断，单 bar 最大 |ret| 为 9.2%。

**解读**：引擎 1 的 +19.52% 落在两个独立口径（+20.31% / +18.21%）之间，方向性结论
（策略在固定 8bps 下仍盈利、Sharpe ~0.53–0.57）跨引擎稳健；但其"恒定权重却不付
再平衡成本"的记账假设系统性偏乐观约 1.2–1.3pp（相对真实维持目标权重的实现）。

## ④ 复现 / Reproduce

```bash
# 完整交叉验证（首次需 CUDA 重建 OOS 缓存，约 2 分钟；之后秒级）
python tools/crosscheck_v13_engines.py
python tools/crosscheck_v13_engines.py --rebuild     # 强制重建缓存
python tools/crosscheck_v13_engines.py --fee-bps 8   # 自定义费率
```

## ⑤ 产物清单（全部为新增文件）

| 路径 | 内容 |
|---|---|
| `tools/crosscheck_v13_engines.py` | 交叉验证工具（重建 + 双引擎 + 归因） |
| `checkpoints/v13_crosscheck/oos_cache.npz` | 重建的 OOS 预测矩阵 / valid mask / r1h / close |
| `checkpoints/v13_crosscheck/equity_curves.csv` | 8 条净值曲线（13,106 点，按 close index 对齐） |
| `checkpoints/v13_crosscheck/crosscheck_summary.json` | 全部指标、分歧与归因数字 |
| `ENGINE_CROSSCHECK_2026-06-10.md` | 本报告 |

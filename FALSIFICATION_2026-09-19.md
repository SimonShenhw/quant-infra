# 九月闸门终审：v13 / v14 全线证伪（2026-09-19）

> **裁定**：三条预注册证据线在各自到期日全部判死，v14 无骨架，决策表落在"回研究桌"。
> 判据于 2026-07-13 冻结、2026-07-23 完成机读实现与两处签核，**全程未改动一个字**。
>
> **但本文最重要的结论不是那三个死判。** 复盘时做的功效分析显示：**60/90 天的 live 窗口
> 从一开始就没有能力确认或推翻它要检验的那些回测**——三个回测 Sharpe 全部落在各自 live
> 读数的 95% 置信区间内。真正决定性的证据在 live 开跑之前就已经在手上：**诚实试验数下
> 的 DSR——策略 SR 只有"纯靠搜 77 次的运气上限"的 0.42 倍。**
>
> 复盘同时发现并修复了裁决代码自身的一个会翻转结论的缺陷（§3），一并全披露。

---

## 0. TL;DR

| 证据线 | 收窗日 | 判据 A | 判据 B | 裁定 |
|---|---|---|---|---|
| carry | 2026-09-11（60 marks） | 全口径 Sharpe **−1.69**（t=−0.68）❌ | funding PnL **+0.5029%** ✅ | **DEAD** |
| O2 | 2026-09-12（60 marks） | 净 Sharpe **−2.55**（t=−1.03）❌ | gross 日均 w·r **−0.0253%** ❌ | **DEAD** |
| v13 | 2026-09-15（96 marks） | live rank IC **+0.0195**（t=+0.75）✅ | **−16.21% vs EW +30.27%**（−46.48pp）❌ | **DEAD** |

预注册决策表输出：**"back to the research desk; retrain on extended window"**。
原定 v14 = carry + O2，两个都死 ⇒ **v14 连骨架都没有**。

一条命令复现：`python tools/gate_check.py`

---

## 1. 预注册了什么，什么时候冻的

| 日期 | 冻结内容 |
|---|---|
| 2026-07-13 | `ROADMAP_2026-07-13.md` Phase 3：v13 与 carry 的判据、到期日、决策表 |
| 2026-07-23 | 裁决代码 `tools/gate_check.py`（**早于最早到期日 50 天**）；同日用户签核两处歧义 |
| 2026-07-23 | ① carry "Sharpe>0" 注册口径 = **含 funding 的全口径**（ex-funding 仅并排展示）<br>② **O2 gate 补注册**（track 07-14 上线晚于 ROADMAP，在 9/60 记账日时钉死判据） |

为什么裁决代码也要提前冻：判据预注册防的是"标准漂向数据"，但**裁决代码若等到九月才写，
同样有经操作化选择漂移的空间**（等权基准怎么定义、Sharpe 用哪条收益序列、怎么年化）。
所以 07-23 把每个歧义点写进 docstring 钉死：

- **等权基准** = `basket_state` 首日（2026-06-11）20 币等权**买入持有**，无成本（从严），
  缺币按最后可得价冻结 — 与 `RESEARCH_2026-07-02.md` 的 BENCH 行同口径；
- **live IC 判官** = `paper_live_ic.compute_live_ics()` 单一实现（交叉验证的教训：
  同一套算术写两遍必然漂移）；
- **carry Sharpe** = 日净收益 `port_ret + funding_pnl − cost_est` 的 √365 年化。

---

## 2. 三条证据线的完整读数

窗口内（截至各自 gate 日）：

| 账本 | marks | 窗口 | 累计 | Sharpe | maxDD | 胜率 |
|---|---:|---|---:|---:|---:|---:|
| v13 basket | 96 | 06-12 .. 09-15 | −16.21% | −2.88 | 16.5% | 49.0% |
| carry（全口径） | 60 | 07-14 .. 09-11 | −6.91% | −1.69 | 17.7% | 43.3% |
| O2 | 60 | 07-15 .. 09-12 | −2.35% | −2.55 | 4.2% | 43.3% |
| combo | 64 | 07-14 .. 09-15 | −5.28% | −2.28 | 10.1% | 40.6% |

**窗外延续（非判据，仅供研究参考）**：v13 到 09-18 已 −25.14%，同期 EW **+47.09%**，
差 72pp。live 这三个月是一轮暴力牛市——这一点对 §4.2 的解读很关键。

### 2.1 各条判据的具体成色

**v13 clause A（live IC > 0）"通过"，但它通过的是一个它无法失败的检验。**
IC = +0.0195，t = +0.75 —— 与零不可区分。这条判据的设计意图是检验信号是否存活，
实际测到的是"96 天不足以把 0.02 和 0 分开"。

**v13 clause B（跑赢等权）是一条 beta 检验，不是 alpha 检验。**
v13 是美元中性多空账本，EW 是纯多头 beta。在 +30% 的上涨行情里，一个中性账本
"跑输纯多头"几乎是构造上的必然，与它有没有 alpha 关系不大。⚠️ **这不构成翻案理由**
（判据已预注册并签核，裁定照旧），但它是下一次设判据时必须修掉的缺陷 — 见 §8。

**carry 的两条判据指向相反方向，且分歧有信息量。**
funding 收入全程为正（+0.5029% / 60 天，**机制本身兑现了**），死的是价格腿：
ex-funding 的 Sharpe 是 −1.81，全口径 −1.69 —— funding 收入只把亏损抹平了 0.12 个
Sharpe 单位。**收 funding 是真的，但为了收它而持有的头寸更贵。**
⚠️ 口径披露（2026-09-24 补）：paper trading 的 K 线取自**现货**（`run_paper_daily.py` 的
`SYMBOLS` 为 `BTC/USDT` 格式；训练 lake 同样来自 `data/spot/monthly`），funding 取自**永续**
（`f"{sym}:USDT"`）。即价格腿按现货标价，与真实永续持仓相差一个基差项。日频下量级小、
方向不定，**不改变裁定**；但严格地说，这条 live 轨迹并不是可以直接下单的永续策略本身。

**O2 的 gross 从未达到名义目标，但这不是实现偏差。**
实测 gross 均值 **0.381**（区间 0.200–0.525），66 天完全没有向 1.0 爬升的趋势。
原因是构造本身：GP 部分调仓 `w ← (1−τ)w + τ·target`（τ=0.2）作用在**每日旋转**的
单位 gross 目标上，平滑后的稳态 gross 由目标的自相关决定，只有目标恒定时才会收敛到 1.0。
回测端用的是同一套构造（ckpt `construction: {continuous_gp, tau 0.2, gross 1.0}`，
`research_objectives.py` 的 "flat-8bps GP(1/5) backtest"），**所以 live 与回测一致、
裁定干净**。⚠️ 但 `health_check` 打印的 "(target 1.0, GP ramp)" 措辞有误导性——
它让 0.32 读起来像"还在爬坡"，实际是稳态。该标签已修（见 §7）。

---

## 3. 全披露：裁决代码有一个会把结论翻过来的缺陷（已修，commit `e9baeac`）

**症状**：`gate_check.py` 取账本行时**没有上界日期**，每次运行都用"到今天为止"的全部行
重判。过了 gate 日之后，注册结论就变成了窗外数据的滑动平均。

**它真的咬了**。carry 全口径 Sharpe 随窗长漂移：

| 窗长 | 截止 | Sharpe | 裁定 |
|---:|---|---:|---|
| 59 marks | 09-10 | −2.07 | DEAD |
| **60 marks** | **09-11（注册收窗）** | **−1.69** | **DEAD ← 这是判据** |
| 61 marks | 09-12 | −1.38 | DEAD |
| 67 marks | 09-18 | **+0.03** | **ALIVE** ← 工具当天的错误输出 |

2026-09-18 运行时，工具打出 **carry ALIVE**，并把决策表推进到
"carry 骨架、模型降为 overlay"——**用 7 天窗外数据复活了一个自己的注册窗口已经判死的
sleeve**。若照此执行，等于在全线证伪的当口重启一条死掉的策略线。

**根因与教训**：**预注册判据 = (指标, 阈值, 窗口) 三件套。窗口丢了和阈值被挪是同一类
错误，只是看起来什么都没发生。** 07-23 花了力气钉死前两项（基准定义、年化口径、
IC 判官单一实现），却漏了第三项——而这段代码存在的唯一理由就是防这类漂移。

**修复**：每条判据在 `min(gate_date, today)` 硬截断——账本行、等权基准的 mark、
以及 IC 日对（日对需要 t+1 收盘价，故以 `date_t1 <= cutoff` 为准）。窗外行仍然打印，
但明确标注 out-of-window、零决策权重；末尾强制打印 REGISTERED VERDICT 汇总块，
保证屏幕上最后一句是注册结论而不是窗外读数。

**为什么写进报告而不是悄悄修掉**：这个缺陷的存在时间覆盖了全部三个 gate 日，任何人
（包括未来的自己）拿 09-12 到 09-18 之间的任一次输出都会看到不同结论。不披露，这份
证据链就有一个无法从产物追溯的空洞。

---

## 4. 为什么失败——四层诊断

### 4.1 排名空间 ≠ 美元空间（2026-07-13 已确立，live 只是兑现）

扩窗重训（`RESEARCH_2026-07-13_extended_window.md`）的结论在 live 之前就写下了：

- 排序信号是真的且穿越全周期：2021–2026 **每一年** rank IC 为正（0.041–0.092），OOS 0.076；
- 但 banded top-3 在全周期亏 **−64.3%**（Sharpe −0.40，maxDD 75%）；
- CTRL 实验：同窗口同配方，仅去掉 4 个晚上市山寨 + funding 因子，
  **+32.6% 直接翻成 −13.6%** ⇒ v13 回测利润高度集中在 APT/ARB/OP/SUI 的尾部交易；
- 决定性诊断 **w·y24 = −0.033%/天**（label 对齐、无成本、无路径）：
  **模型在排名空间赢，在美元空间输**——小幅度的对子排对了，大波动的名字放错了边。
  ⚠️ 2026-09-24 修正：该值 t = −1.06，统计上与 0 不可区分；主因是离散度择时（58%），"大波动放错边"只是次要渠道（42%）。见 §6ter。

v13 的 live 表现（空头腿在强势币上被轧）是这一结论的实时重演，不是新信息。

### 4.2 因子层：没有衰减，但也没有稳定性

`tools/factor_decay_monitor.py`（lake 补到 2026-08-31 后，live 重叠 **81 天**，首次有功效）：

| 因子 | 回测 IC | live IC | z | 备注 |
|---|---:|---:|---:|---|
| std20 | −0.0361 | **+0.0273** | +1.97 | 回测最强因子，**符号翻转** |
| btc_dominance | +0.0154 | **−0.0878** | **−3.55** | 全表唯一 \|z\|>3 |
| trade_imbalance | −0.0170 | +0.0512 | +2.24 | 翻转 |

因子排序存活度：rank corr(回测 IC, live IC) = **−0.441**，20 个因子里只有 8 个保住符号。
⚠️ 但必须并排给出**衰减天花板 +0.07**——以当前噪声，即便排序完全延续，观测相关也只该
是 +0.07 量级；而 20 个相关因子下该统计量的 SE ≈ 0.23。**所以 −0.441 提示的是"反转
而非衰减"，但不决定性。**

**关键观察：IC 没有变小，是变号了。** 好几个因子的 \|IC\| 在 live 端反而更大
（btc_dominance 0.015→0.088，trade_imbalance 0.017→0.051）。这排除了"因子被市场
套利掉了"这一类解释。

### 4.3 一个被提出、检验、然后否决的假设

**假设**：live 窗口是牛市（EW +47%），而回测窗口是震荡/下跌（同期 EW −26.9%）；
趋势/波动类因子的 IC 符号随 regime 翻转，所以这是 regime 错配而非因子失效。

**检验**：按 BTC 收盘价对其自身 200 日 SMA（因果，不含当日）把
2023-05-03 ~ 2026-08-31 的 **985 个记账日**拆成 up（614）/ down（371），逐因子算条件 IC。

**结果：假设被否决。**

- **没有任何因子 \|z\| > 2**（最大的是 std20，z = −1.73）；
- 而且**方向与假设相反**：up regime 下 std20 的 IC 是 **−0.0269**（更负），
  down regime 是 +0.0032；而在 live 那段牛市里它是 **+0.0273**。
  **同一个 regime 标签给出相反的符号** ⇒ 这个 regime 指标解释不了 live 的翻转。

脚本 `tools/regime_ic_check.py` **已入库**——留着不是因为假设活下来了，而是为了让
这个**否定结论**可复现。它是对已被证伪系统的诊断，不算 selection trial，
刻意不进 `trials.json`；若日后据此改模型，那就是 trial，必须注册。

### 4.4 一开始就成立、且不依赖 live 的那个统计量

用仓库自己的 `tools/validation_stats.py` 在诚实试验数下重算（从
`checkpoints/v13_backtest_summary.json` 精确反解，n=50 复现存档值误差 **2.06e-10**）：

| n_trials | E[max SR]（零假设下纯运气上限） | DSR |
|---:|---:|---:|
| 50（`run_v13_final.py` 硬编码常数） | 0.019080 | 0.1129 |
| 56（registry historical_base） | 0.019441 | 0.1051 |
| **77（`trials.json` 诚实计数）** | **0.020429** | **0.0857** |
| 100 | 0.021211 | 0.0724 |
| 200 | 0.023181 | 0.0459 |

**策略单期 SR = 0.008607，而 n=77 下零假设的 E[max SR] = 0.020429 ⇒ 比值 0.421。**

即：**回测的夏普只有"什么信号都没有、纯靠搜 77 次所能撞出的最好成绩"的 42%。
它从来没有过自己试验数设的那道线。** 这个判断在 2026-06 就可以做出，**完全不需要
任何 live 数据**。

---

## 5. 功效分析：这些 gate 本来就不可能确认任何东西

这是复盘中最不舒服、也最有价值的发现。年化夏普由 n 个日度观测估计时，
SE(SR_ann) ≈ √(365/n)。代入各自的注册窗口：

| sleeve | n | SE(SR_ann) | live SR | live 95% CI | 回测 SR | 回测值落在 CI 内？ |
|---|---:|---:|---:|---|---:|:---:|
| v13 | 96 | 1.95 | −2.88 | [−6.70, +0.94] | 0.81 | **是** |
| v13（保守口径） | 96 | 1.95 | −2.88 | [−6.70, +0.94] | 0.53 | **是** |
| carry | 60 | 2.47 | −1.69 | [−6.52, +3.14] | 0.53 | **是** |
| O2 | 60 | 2.47 | −2.55 | [−7.38, +2.28] | 0.138 | **是** |

**四个回测值全部落在各自 live 读数的 95% 置信区间内。** 换句话说：
**live 数据在统计上没有推翻任何一个回测。**

反过来看，要在 2σ 下把一个真实夏普与零分开需要多少天：

| 真实 SR_ann | 需要 n | 折合 | 来源 |
|---:|---:|---:|---|
| 0.810 | 2,225 天 | **6.1 年** | v13 回测头条 |
| 0.530 | 5,198 天 | **14.2 年** | v13 保守口径 / carry 回测 |
| 0.138 | 76,665 天 | **210 年** | **O2 回测——即那个被选为 v14 模型骨架的 sleeve** |

注册的窗口是 **60 天和 90 天**。

**这意味着三件事：**

1. **这些 gate 只能捕捉灾难，不能确认优势。** 它们确实捕捉到了不好的表现
   （v13 三个月亏 16% 而市场涨 30%，这是真金白银的经济事实），但"DEAD"在这里的准确
   含义是"没能越过设定的栏杆"，**不是"已证明没有 alpha"**。
2. **O2 这条尤其站不住。** 它在 07-13 被选为 v14 模型骨架时，自身的回测成绩是
   **+4.63% / Sharpe 0.138 / PSR 0.62（2021–2026 五年半）**。07-23 为它注册 60 天 gate
   时**没有做功效计算**——一个 Sharpe 0.138 的策略需要两百年才能与零区分，60 天的
   "净 Sharpe > 0"实质上是抛硬币。
3. **真正该起决定作用的证据不是 live，是 §4.4 的 DSR**，而它在 live 开跑前就已成立。
   这三个月跑的是一个没有功效的确认实验，去检验一个早已被自己的选择偏差统计判否的假设。

⚠️ **这一节不构成翻案**。判据已预注册、已签核，裁定就是 DEAD，本文不改判。
这一节的用途是：**如果还有下一次，判据必须先过功效关再进 registry。**
同时诚实地指出反向推论也存在——"gate 欠功效"确实也可以推出"应该跑一个功效足够的
新实验"，但那是一个**新决策**（要不要开新测试），不是对本次的重判。

---

## 6. 这次确立了什么、没确立什么

**确立了（高置信）：**
- 三条 sleeve 在各自注册窗口内均未越过预注册栏杆；v14 无骨架。
- 经济事实：96 天 −16.21%，同期等权基准 +30.27%（窗外延续到 −25.14% vs +47.09%）。
- v13 回测的夏普从未达到其自身试验数所设的运气门槛（SR / E[max] = 0.42 @ n=77）。
- v13 回测利润集中于 4 个晚上市山寨的尾部交易（CTRL 实验：+32.6% → −13.6%）。
- 模型在排名空间有信号、在美元空间没有（w·y24 全周期为负）。

**没有确立（不要对外这样讲）：**
- ✗ "live 证明了模型没有 alpha" —— live 窗口无此功效（§5），四个回测值都在 CI 内。
- ✗ "因子衰减了" —— 因子 \|IC\| 没变小，是变号；且变号本身未达决定性显著。
- ✗ "是 regime 错配" —— BTC-200d-trend 版本的这个假设已被数据否决（§4.3）。
- ✗ "carry 机制失效" —— funding 收入全程为正；死的是价格腿。

**最诚实的一句话总结**：这套因子从头到尾在可探测边缘工作——全表 \|IC\| 在 0.01–0.09
之间而 SE 是同一量级（live 端连 std20 的 +0.0273 自己都不显著于零）。在这种信噪比下，
回测里那个 +32.6% 是选择偏差的产物（DSR 早就这么说了），而 live 的 −16% 与之并不矛盾。

---

## 6bis. 补遗（2026-09-20）：§6 "没有确立"那一栏，填掉了一条

原 §6 列出本项目**从未证明过**的一件事：**若真有信号，这套管线能否找到并变现它。**
在此之前每个负结果都在"市场没信号"和"有信号但管线转不成"之间二义。

`tools/pipeline_calibration.py` 用植入信号做了这个检验：把**已知强度**的合成分数喂进
**真实的 config-C 回测路径**（banded top-3 / enter<3 exit≥6 / TWAP 成本），
20 币钉死宇宙、28,453 小时样本、5 seed。

- **NULL 臂（纯噪声）：中位 Sharpe −0.58。** ✅ 机器不会凭空造钱。
- **盈亏平衡 rank IC ≈ 0.024。** 超过它就赚：IC 0.036 → Sharpe +1.19；IC 0.070 → +3.56。

**⇒ 管线不是瓶颈，二义性消解：问题在信号，不在构造。** 而且整个故事能压成一句话：

| | rank IC | 相对平衡线 0.024 | 结果 |
|---|---:|---:|---|
| v13 回测 | 0.064 | 2.6× | +32.6% |
| v13 live（96 marks） | 0.0195 | **0.80×** | −16.21% |

**平衡线正好夹在回测与 live 之间。** 无需"regime 变了""因子死了"之类的额外假设——
只需要"回测 IC 被选择偏差抬高约 70%"，而这正是 DSR 早就说过的（§4.4，0.42 倍）。

⚠️ 三条限制必须同时引用：① **0.024 是下界**（合成信号质量恒定，真实信号 IC 时变、
更难变现）；② 窗口/宇宙与 v13 回测不同；③ 高 IC 端总收益是无容量约束的复利、无意义，
只读 Sharpe。

⚠️ **另一个假设在此被否**：为复现 §4.1"大波动放错边"而设的 SMALLMOVE 臂**没能复现它**
——匹配实测 IC 后两臂平衡点几乎相同（0.0244 vs 0.0238）。w·y24<0 的成因**仍未解释**（→ 已于 2026-09-24 在 §6ter 解释）
（可能因为那个诊断用分数比例权重、此处用只看排名的 banded top-3）。别把它讲成已解释。

⚠️ 过程中发现的新隐患：`run_v13_final.build_from_parquet` 的宇宙是
`sorted(lake_symbols)[:max_assets]`，**lake 长大后会静默返回不同的 20 个币**
（现吃进 FET/FIL/PEPE/RENDER、丢掉 SOL/SUI/UNI/XRP）。已加可见性守卫（打印解析出的
宇宙），但**任何复跑 v13 的人必须先确认宇宙**。同款失效在 qr-alpha-lab 的生存偏差
案例里独立出现过（静态宇宙 Sharpe +0.82 → 时点宇宙 −0.01）。

文献对照与外部独立平行项目见 `RESEARCH_2026-09-20_literature.md`。

---

## 6ter. 补遗（2026-09-24）：w·y24 悬案关闭——预注册分解

§4.1 与 §6bis 留下的唯一未解项是："rank IC 每年为正，为什么 rank 权重下的 w·y24 为负？"
这次按预注册做了 2×2 分解。预注册文件 `PREREG_2026-09-24_wy24_decomposition.md`
（commit `fbafd93`）**先于运行提交**；工具 `tools/wy24_decomposition.py`；
结果存于 `checkpoints/wy24_decomposition.json`。

**复现**：在前 45,227 行上，A0 与存档值**逐位一致**（rank −0.0003343386，rank/vol
−0.0000877068，误差为 0），说明输入对齐无误。

**结果**（1,885 个不重叠决策日，2021-01 至 2026-03）：

| 格 | 含义 | 均值 %/天 | NW t(5) |
|---|---|---:|---:|
| A0 | 原始 y，保留离散度（即观测值） | −0.0334% | −1.06 |
| A1 | 原始 y，去掉离散度择时 | +0.1397% | +5.74 |
| A2′ | 名次化 y，保留离散度 | +0.0945% | +2.53 |
| A2 | 名次化 y，去掉离散度（即 IC 应得的 P&L） | +0.2361% | +10.11 |

日均 Spearman IC 为 +0.0720（NW t = +10.11）。IC 应得而没有得到的部分 A2 − A0 = +0.27%/天，
按 Shapley 分摊：离散度择时 (b) +0.157%/天，占 **58%**；收益端尾部 (a) +0.112%/天，
占 **42%**。两种移除顺序下都是 (b) 大于 (a)。
A3：IC_t 对 σ_t 回归，斜率 −1.42，NW t = **−3.06**，即离散度越大的日子 IC 越低。

**预注册预测的核对：**

| | 预测 | 置信度 | 结果 |
|---|---|---|---|
| P1 | A0 与 0 不可区分 | 约 60% | ✅ 成立（t = −1.06） |
| P2 | 尾部份额大于离散度份额 | 约 55% | ❌ **被否**（42% 对 58%） |
| P3 | 离散度越大，IC 越低 | 约 60% | ✅ 成立（t = −3.06） |

**三个结论：**

1. **"w·y24 < 0"这个前提本身不成立。** −0.0334%/天 的 t 值只有 −1.06。§4.1 和扩窗研究
   的准确说法应当是："**高度显著的 rank IC（t = +10），被衰减成了统计上为零的美元 P&L**"，
   而不是"符号翻转"或"在美元空间输"。
2. **衰减的主因是离散度择时，而不是"大波动的币站错了边"。** 模型的 IC 在离散度大的日子
   显著更低（A3），而美元 P&L 恰好按离散度给每一天加权，于是模型表现最差的那些天分量最重。
   07-13 的原诊断（收益尾部）是**次要**渠道，占 42%；我的 P2 押错了。
   - 最典型的是 2021 年：离散度 4.98%（其余年份在 1.5–2.6% 之间），IC +0.085 为各年最高，
     A0 却是 −0.18%/天；一去掉离散度择时，A1 就变成 +0.10%/天。
3. **它顺带解释了之前留下的两处尾巴：**
   - SMALLMOVE 臂为什么没能复现（§6bis）：它是在**同一天之内**沿 |y| 挪动准确度，
     而主渠道是**跨天**的。
   - 植入信号标定的平衡线 0.024 为什么只是下界（§6bis 限制 1）：合成信号每一天的质量都相同，
     不存在离散度择时；真实信号恰恰在高离散度日更差，所以需要更高的平均 IC 才能保本。

**⚠️ 从这里读不出的东西：** A1（+0.14%/天，t = 5.74）**不是一个策略**。σ_t 是**未来**
24 小时收益的横截面离散度，用它给仓位定尺度就是前视。A1 只是提示了一个可以检验的假设：
按**预测**的离散度倒数给账本定尺度。但那是一个**新 trial**，必须先预注册、再过功效准入。
本项目唯一没碰过的留出期是 lake 新增的 2026-04 至 08，约 5 个月；要在这么短的窗口里做到
t = 2，需要 SR ≥ 3.1。按同日新增的功效准入（`register_trial`，不变量 t7），这样的检验
会被拒绝登记，除非明确标为探索性。

---

## 7. 运维记录（基建侧）

100 个连续记账日，**零断档**（`health_check` 30 日窗口 gaps = none，账本日期连续）。

| 项 | 实测 |
|---|---|
| v13 账本跨度 | 2026-06-12 .. 2026-09-19 = 100 marks / 100 日历天 |
| `run_meta` 覆盖 | 2026-07-14 .. 2026-09-19（68 天），57 行 |
| 其中由补课推进的日子 | **11 天**（07-18, 07-22, 07-27, 08-05, 08-09, 08-17, 08-25, 09-02, 09-07, 09-09, 09-14）——全部 11 天仍产出了记账行 |
| 运行错误 | **0** 条 `run_meta.notes` |
| funding 退化 | **0** 天（57 行全部 `[]`） |
| 不变量 | **6/6** 通过 |
| 数据库备份 | 10 份 |

⚠️ **需要披露的口径影响**：那 11 天是调度未触发（机器关机/锁屏）后由自愈补课补上的，
补课路径按设计**冻结持仓**、只补 PnL 标记。因此 live 序列并非"每天都重新调仓"，
与回测的每日调仓假设有轻微偏离（少付了换手成本，但持有了过期仓位）。方向不确定，
量级小，但应记录在案。

另：`health_check` 的 O2 gross 标签已由误导性的 "(target 1.0, GP ramp)" 改为如实描述
稳态（§2.1）。

---

## 8. 如果还有下一次：三条硬规则

1. **判据进 registry 之前先过功效关。** 写下"我要检验的效应量是多少、这个窗口能不能
   在 2σ 下看见它"。看不见就别注册——要么加长窗口，要么换一个有功效的判据
   （例如直接检验 w·y24 这类日频、label 对齐的量，而不是检验年化 Sharpe 的符号）。
2. **判据是 (指标, 阈值, 窗口) 三件套，裁决代码必须硬截断窗口。** 见 §3。
3. **基准要与策略同类。** 用纯多头 beta 去卡一个美元中性账本，测的是 beta 不是 alpha
   （§2.1）。中性策略的合理基准是零、是同类中性策略、或是显式的
   "扣除 beta 后的超额"，不是买入持有。

并且：**DSR 应该前置成准入条件，而不是事后报告项。** 如果 2026-06 就把
"SR 必须超过 E[max SR] @ 诚实试验数"当作开跑 live 的**前置门槛**，这三个月的 live
根本不会启动——那个数当时就已经是 0.42 倍。

---

---

## 9. 收摊记录（2026-09-20）

本项目的 live 阶段于 2026-09-20 正式停止并归档。

**账本终值（冻结）**

| 表 | 行数 | 覆盖 |
|---|---:|---|
| `basket_pnl` | 100 | 2026-06-12 .. 2026-09-19 |
| `carry_pnl` | 68 | 2026-07-14 .. 2026-09-19 |
| `o2_pnl` | 67 | 2026-07-15 .. 2026-09-19 |
| `combo_pnl` | 68 | 2026-07-14 .. 2026-09-19 |
| `run_meta` | 57 | 2026-07-14 .. 2026-09-19 |

`PRAGMA integrity_check` = **ok**。

**防篡改归档**：`archive/paper_daily_FINAL_2026-09-20.db`（679,936 bytes），
SHA-256 记于 `archive/SHA256.txt` 并已入版本控制（DB 本体按 `.gitignore` 不入库）：

```
18569523573bf24d272db3485fdb562b4319c606f8c13d4da97dae74c2f044aa
```

⚠️ **这个哈希是有用途的，不是仪式**：注册窗口内的账本行是九月裁决的证据。
任何日后对 `paper_daily.db` 的追加或修改都会让它与上面这个值不符，
从而**可被发现**。引用本报告任何数字之前，可先核对哈希。

**已停止的东西**

- 计划任务 `QuantInfra每日模拟盘`（每日 19:30）→ **Disabled**
- 计划任务 `QuantInfra看门狗`（每日 21:00 deadman）→ **Disabled**

两者都是**禁用而非删除**，随时可在任务计划程序里重新启用。
最后一次运行均成功（LastTaskResult = 0），桌面无 `PAPER_RUN_FAILED.txt` 哨兵。

**停止后的行为变化（需要知道，否则日后会困惑）**

1. **告警链已随看门狗一起关闭。** 账本不再推进是**预期状态**，不再是故障，
   所以桌面哨兵不会再出现——**别把"哨兵没出现"当成系统健康的证据**。
2. `python tools/health_check.py` 从此会随天数增长报 `CRITICAL: freshest ledger
   is N days old`。**这是正确行为**（它在如实报告停摆），不是新 bug。
   本项目归档后，该工具的退出码不再具有运维含义。
3. ⚠️ 桌面 `每日模拟盘.bat` **仍然存在且仍可运行**（未删除，那是用户自己的文件）。
   手动双击它会向已归档的证据账本**追加窗口外的行**。真要重启研究，
   请先复制一份 DB 另起炉灶，不要在这本账上继续写——上面那个哈希就是为此存在的。

**仍未 push 的东西**：无。截至本次收摊，本地 master 与 origin 同步。

## 附录 A：复现

```bash
python tools/gate_check.py            # 三条线的注册裁定（窗口已硬截断）
python tools/health_check.py          # 账本连续性、备份、gate 倒计时
python tools/paper_live_ic.py         # live rank IC（注册判官）
python tools/factor_decay_monitor.py  # 逐因子 live-vs-回测 IC（需 lake 到 2026-08）
python tools/regime_ic_check.py       # regime 条件 IC（§4.3 那个被否决的假设）
python tests/run_invariants.py        # 6 个不变量
```

关键产物：`paper_daily.db`（四本账 + `run_meta`）、`checkpoints/v13_backtest_summary.json`、
`checkpoints/research_objective_O2.json`、`trials.json`（n_trials=77）。

## 附录 B：已知口径与局限

- **lake 的 2026-03 是残月**（止于 03-30 02:10，初始拉取日），刻意保持原样——
  那是 v13/O2 实际训练所用数据的忠实历史。⚠️ **永远不要把当前月（残月）写进 lake**：
  下载器的 skip-if-exists 会把它永久冻结为残缺。
- **标记语义**：live 当天路径按运行时刻快照（约 23:30 UTC）标价，补课路径与所有 lake
  侧工具按 23:00 整点收盘标价。跨来源比价会看到 40–500bps 差异，那是这约 30 分钟的
  价格移动，**不是数据错误**；两条序列各自内部自洽。运行时刻可从 `run_meta.run_utc` 回溯。
- **funding 原始费率库止于 2026-03-31**（模拟盘只持久化 carry 信号、不存原始费率），
  故 `funding_rate` 因子在衰减表中 live 端为 n/a，而非伪造的 0。
- **regime 检验窗口从 2023-05 起**，因为 live 20 币宇宙含晚上市币种，全 lake 交集只能
  从那时开始——未覆盖 2021 狂热年。

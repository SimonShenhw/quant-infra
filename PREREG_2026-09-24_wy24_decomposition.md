# 预注册：w·y24 分解（Track A，2026-09-24）

> 本文件在运行分解**之前**提交。它的 git 提交早于结果的提交，这就是"预测先于数据"的证据。
> 这是对已被证伪系统的诊断，不是 selection trial，**不进 `trials.json`**。

## 问题

扩窗研究（`RESEARCH_2026-07-13_extended_window.md`）报告了两件事：
- rank IC 在 2021–2026 年**每一年**都为正；
- 但 rank 权重下 24h 对齐的毛 P&L（w·y24）是 **−0.0334%/天**。

`FALSIFICATION_2026-09-19.md` §6bis 把"w·y24 < 0 的成因"列为唯一的未解项。

## 已核实的输入

| 项 | 来源 |
|---|---|
| 预测 | `checkpoints/preds_ext_full.npy`，45,227 × 16，float32，全部有限 |
| y24 | `tools/research_extended_window.build_window(WINDOWS["FULL"])`，**取前 45,227 行**。lake 已延长到 2026-08，多出来的行没有对应的预测 |
| 权重 | `tools/research_rank_weighted.rank_weights`（单一实现，原样导入） |
| 采样 | 每 24 小时取一个样本（`DECISION_EVERY = 24`），样本之间不重叠 |

## 复现门槛（不过就停）

在前 45,227 行上，A0 必须复现存档值（`checkpoints/research_rank_weighted.json`），误差 < 1e-8：
- rank 权重：**−0.0003343386**
- rank/vol 权重：**−0.0000877068**

复现不了就**停**，先查输入的对齐。

## 2×2 分解

对每一天 t：w_t = rank_weights(s_t)，σ_t = y_t 的横截面标准差（ddof = 0），
σ̄ = σ_t 的样本均值，q̃_t = y_t 的横截面名次，中心化后缩放到单位标准差。

| | 用原始 y（保留收益尾部） | 用名次化的 y（去掉收益尾部） |
|---|---|---|
| **保留离散度择时** | **A0** = w·y | **A2′** = w·q̃·σ_t |
| **去掉离散度择时** | **A1** = w·(y/σ_t)·σ̄ | **A2** = w·q̃·σ̄ |

四格都取对 t 的均值，单位统一为"收益"，都能与 A0 直接比较。

**A2 是 IC 本来应该带来的 P&L**：名次对名次，每天等权，按平均离散度折算。
A2 − A0 就是"显著的 IC 没能变成的那部分钱"。它在两个渠道之间按 Shapley 分摊：

- 收益尾部 (a) = ½ [ (A2 − A1) + (A2′ − A0) ]
- 离散度择时 (b) = ½ [ (A1 − A0) + (A2 − A2′) ]
- 两者之和恒等于 A2 − A0

**A3** 另作直接检验：把每日 Spearman IC_t 对 σ_t 做回归，报告斜率的 Newey–West t 值。

所有均值都报告 Newey–West HAC t 值（lag 5，另报 lag 10 作稳健性检查）。另附逐年拆分。

## 预测（在看到任何结果之前写下）

**P1：A0 在统计上与 0 不可区分，即 |HAC t| < 2。** 置信度约 60%。
- 依据：日 P&L 波动粗估 0.7–1.0%；约 1,884 个不重叠日 ⇒ 标准误约 0.02%，t 在 −1.4 到 −1.9 之间。
  存档的逐年数字从 −0.18%（2021）摆到 +0.11%（2025），波动很大。
- 若 P1 成立：07-13 那个"决定性诊断"的前提就不成立。准确的说法应改为
  "**高度显著的 rank IC，被衰减成了统计上为零的美元 P&L**"，而不是"符号翻转"。
  分解解释的是这部分衰减。

**P2：收益尾部 (a) 在 Shapley 分摊中的份额大于离散度择时 (b)。** 置信度约 55%。
- 依据：07-13 的原始诊断是"大波动的币站错了边"。但在暴涨日，两个渠道会**同时**起作用
  （一个币暴涨，既制造了横截面尾部，也抬高了当天的离散度），所以事先很难分开。
  这条预测接近抛硬币，照实写下。

**P3：A3 的斜率为负，即离散度越大的日子，IC 越低。** 置信度约 60%。
- 依据：07-13 的逐年表显示，尾部空头在狂热年（2021）亏得最多。

**P0（机械性的，不算预测）：** A2 > 0。因为逐年 rank IC 全为正，这一格按构造应为正。
如果 A2 ≤ 0，说明实现有 bug，**停**。

## 判读规则

| 观测 | 结论 |
|---|---|
| A0 复现失败，或 A2 ≤ 0 | 实现或输入有问题，**停** |
| A0 的 HAC t ≤ −2 | 符号翻转是真的，分解解释的就是这次翻转 |
| A0 的 \|HAC t\| < 2 | 前提不成立；把结论改成"IC 被衰减到零"，分解解释的是这部分衰减 |
| (a) 份额 > 50% | 主要问题是模型把大波动的币放错了边 |
| (b) 份额 > 50% | 主要问题是模型在离散度大的日子里判断更差 |

**无论结果如何，都写进 `FALSIFICATION_2026-09-19.md` 的补遗。** 预测错了也照实记录。

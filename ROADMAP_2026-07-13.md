# ROADMAP（2026-07-13 预注册）

> 目的：把 v14 的判据在证据到齐**之前**写死，避免事后合理化——与报 DSR 的逻辑同源。
> 三条证据线约 2026-09 中旬同时到齐。

## Phase 0（本周）：carry paper track 上线

- `paper_daily.db` 新增 `carry_state` / `carry_pnl`（与既有 `basket_state`/`basket_pnl` 命名对称），外加纯记账的 `combo_pnl`（50/50 虚拟账本）。
- 每日任务新增 step：3 日均 funding → 反向排序 → banded top3/bottom3（enter<3 / exit≥6，与模型篮子同参）→ 记账（价格 PnL + funding 计提 + 槽位成本估计）。
- 数据零新增依赖：bars + funding 已在每日流程内。
- carry 不依赖 GRU 模型 → 无模型过拟合风险；当前强势币 regime 恰是其理想检验环境。
- **每晚一天 = 少一天 live 样本。**

## Phase 1（1–2 周）：sleeve 最小重构

- 抽象两个东西，不多不少：
  - `Sleeve`: `name / compute_scores(bars, funding, date) / update_basket(prev, scores)`；`ModelSleeve(v13 ckpt)` 与 `CarrySleeve(3d funding)` 为同一接口两实例。
  - `PortfolioBook`: 每 sleeve 一本账 + combo 账（净额、成本、funding 计提）。
- `run_paper_daily.py` 收敛为：fetch 一次 → 遍历 sleeves 出信号记账 → combo 记账 → 落库。
- **banded top-K 组合构建抽成共享模块**，回测端与模拟盘端跑同一份代码（crosscheck 报告 ±1pp 引擎分歧部分来自双实现）。
- 明确不做：不动 `engine/` EventBus 底层；不为重构而重构。
- 顺手：trial registry（`trials.json`，研究脚本自动 append，`validation_stats.py` 读取算 DSR n_trials，替代手工计数）。

### 横切（跟 Phase 1 走，不单独立项）：不变量回归测试 ×4

两个历史致命 bug（随机权重、µs 时间戳）共同点 = 静默失败。要的不是覆盖率，是不变量：
1. 时间戳归一化 golden test：已知 µs CSV → 断言聚合 bar 数；
2. no-lookahead 自动化：t 之后数据换噪声 → 断言 t 时刻因子值不变；
3. checkpoint 指纹断言推广到所有入口（paper daily 的 val_corr 校验已是雏形）；
4. `basket_step` 记账守恒小测试。
改共享模块前必跑。

## Phase 2（并行，2–4 周）：regime 覆盖——v14 模型侧主攻

按性价比排序：
1. **数据扩窗**：19 个月 → 2021 起。现回测窗口（2024-09~2026-03）整体 -26.9%，模型没见过持续上涨 regime；成本主要在动态 universe（早期部分币未上市）。直接检验"空头 alpha = 熊市伪影"假设，优先于一切模型改动。
2. **Regime 条件分析**：BTC 200d 趋势 / 已实现波动分桶，把现有 OOS 预测的 IC 按 regime 拆开。若空头腿在上涨 regime 系统性负贡献 → v14 gate 空头腿或按 regime 缩 gross。
3. Meta-labeling（López de Prado）：候选，排在 1、2 之后。

## Phase 3（gate 驱动，~9 月中）：v14 go/no-go

**预注册 gate（不得事后修改判据）：**

| 证据线 | 到期 | 通过判据 |
|---|---|---|
| v13 live | 满 90 天（~09-15） | live IC > 0 **且** 净值跑赢等权基准 |
| carry live | 满 60 天（07-13 起 → ~09-11） | Sharpe > 0 **且** funding PnL > 0 |
| 扩窗回测 | Phase 2 完成 | 空头腿 alpha 在上涨 regime 是否存活 |

**决策表：**
- 两 live 都活 → v14 = 多 sleeve 组合（E 的线性叠加近似换成 PortfolioBook 级资金复用与净额结算）。
- v13 死、carry 活 → carry 为骨架，模型降为 overlay。
- 都死 → 回研究桌，扩窗数据重训。

## 运维规程（2026-07-14 终审后新增）

**符号退市 SOP（终审 G1——恢复路径陷阱）**：若某币从三所同时消失，系统会因新鲜度守卫/全齐守卫硬失败并在桌面立哨兵（这是正确行为）。**不要直接把该币移出 SYMBOLS**——banding 会让持仓"无声蒸发"，退市前的崩盘损益永不确认。正确顺序：① 用最后可得价手工在 state/pnl 表写一行确认退出（计 slot 成本）；② 再改 SYMBOLS；③ 在 run_meta.notes 记录事件。模型 sleeve 的位置嵌入意味着宇宙变更 = 模型需重训，一并评估。

**告警链**：python 非零退出 → bat 写桌面 `PAPER_RUN_FAILED.txt`（成功自动清除）；账本停摆 >3 天 → 看门狗任务（每日 21:00，`QuantInfra看门狗`）写同名哨兵。看见桌面这个文件 = 找 Claude 查 `paper_daily_log.txt` 和 `python tools/health_check.py`。

**待办（需要用户本人操作）**：计划任务当前为 Interactive 登录类型——Windows 强制重启后停在锁屏时不会运行（终审 F2）。若希望完全免维护：任务计划程序 GUI 中把两个 QuantInfra 任务改为"不管用户是否登录都要运行"（需输入 Windows 密码，我无法代办），或确保开机自动登录。当前缓解：登录后 StartWhenAvailable 自动补跑 + 28 天补课额度（okx 截断修复后由 gate 的 1000 根上限支撑）。

## 明确 park

repo 中 LOB 论文指向的日内/微结构方向：另一个数据基建量级（depth 流），日频系统证明自己之前不开。
